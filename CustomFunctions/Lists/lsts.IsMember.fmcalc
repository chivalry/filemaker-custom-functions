// Returns true if the Item is a member of TheList. The surrounding paragraphs
//   marks make sure that we aren't returning true when the item exists as a
//   substring within another item.
//
// Written by Charles Ross

( not IsEmpty( _item ) )
  and
( not IsEmpty( _list ) )
  and
PatternCount( "¶" & _list & "¶"; "¶" & _item & "¶" )
