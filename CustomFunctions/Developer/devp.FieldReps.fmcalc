// Returns the number of repetitions a field has defined regardless of the
//   number displayed on any particular layout.
//
// Written by Charles Ross

Let(
  [
    _fullType = FieldType( Get( FileName ); _fieldName );
    _typeWord = RightWords( _fullType; 1 )
  ];

  GetAsNumber( _typeWord )
)
