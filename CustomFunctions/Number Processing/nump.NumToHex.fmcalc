// 2009-11-04 jsmall
// updated to be FMP10 compatible 
// (had a name conflict with Char function)
// refactored as NumToHex, formerly DecToHex
// (the function only actually supports integers, 
// not "decimal" fractions as the old name implied)

Let ( [
_bit = Mod ( _integer ; 16 ) ;
_alpha = "0123456789ABCDEF" ;
_char = Middle ( _alpha ; _bit + 1 ; 1 ) ;
_next = Div ( _integer ; 16 )
] ;

Case ( 
_next ;
nump.NumToHex ( _next )
)//end Case
& _char
)//end Let
