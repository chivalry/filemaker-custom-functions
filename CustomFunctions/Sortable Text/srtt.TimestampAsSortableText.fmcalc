// Returns a textual version of a timestamp so that when converted to text and
//   sorted the sorting occurs correctly given the timestamp values.
//
// EXTERNAL REQUIREMENTS: The presence of DateAsSortableText and
//   TimeAsSortableText custom functions.
//
// Written by Charles Ross

srtt.DateAsSortableText( GetAsDate( _timestamp ) ) &
  srtt.TimeAsSortableText( GetAsTime( _timestamp ) )
