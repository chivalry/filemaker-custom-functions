// Returns a platform-specific file path given an ID and an extension.

Let(
  [
    _tempFolder = Get( TemporaryPath );
    _fileName = _recID & "." & _extension;
    _filePath = fpth.OSProtocol & _tempFolder & _fileName
  ];

  _filePath
)
