// Unescapes the characters in the string that are used to delimit the
//   dictionary.
//
// Written by Charles Ross

Substitute(
  _string;
  
  [ "/:"; ":" ];
  [ "/="; "=" ];
  [ "/>"; ">" ];
  [ "/<"; "<" ]
)
