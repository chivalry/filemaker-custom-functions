/* this is nicked from python, but a proper useful FMP function 
*/

Case ( json.#VarType ( _list ) ≠ "list" ; json.Null ;

(json.#Count ( _list ) <= 1) ; "<|->" ; // you need two or more lists to zip

not json.#Count ( json.#Get ( _list ; 0 ) ) ; "<|->" ; // empty list supplied

json.|List (  
  json.|Tool (  "ZIP" ; _list ; 0 ;  json.#Count ( _list ) ; 0 ; json.Null ; json.Null )  
)

)
